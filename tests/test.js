jsdomWrap = require('../lib/nodeunit-jsdom')

exports.testWrapper = {
    testNoArgs: function(test) {
        test.done()
    },
    testCustomHtml: function(test) {
        test.done()
    },
    testCustomReqs: function(test) {
        test.done()
    },
    testJsdomError: function(test) {
        test.done()
    }
}
